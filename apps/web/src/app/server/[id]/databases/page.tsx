import { ServerSection } from "@/components/server-section";

export default function DatabasesPage() {
  return (
    <ServerSection
      title="Databases"
      description="Database hosts are not configured on this panel yet. When a host is added, databases for this server will be provisioned here."
    />
  );
}
