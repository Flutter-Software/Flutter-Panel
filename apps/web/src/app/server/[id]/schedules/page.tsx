import { ServerSection } from "@/components/server-section";

export default function SchedulesPage() {
  return (
    <ServerSection
      title="Schedules"
      description="Scheduled power, backup, and console jobs are not enabled yet."
    />
  );
}
